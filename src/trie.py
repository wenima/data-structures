"""Trie data structure."""


class Trie(object):
    """Trie data structure for storing an associative array."""

    def __init__(self):
        """Initialize a trie."""

    def insert(self, string):
        """
        Insert the input string into the trie.
        If character in the input string is already present, it will be ignored.
        """

    def contains(self, string):
        """Return whether input string is in the trie."""

    def size(self):
        """Return number of words within trie."""

    def remove(self, string):
        """
        Remove the given string from the trie.
        If the word doesn’t exist, will raise an appropriate exception.
        """
